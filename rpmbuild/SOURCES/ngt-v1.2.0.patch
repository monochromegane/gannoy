diff --git a/lib/NGT/Graph.cpp b/lib/NGT/Graph.cpp
index cd4928b..eb1a713 100644
--- a/lib/NGT/Graph.cpp
+++ b/lib/NGT/Graph.cpp
@@ -60,8 +60,9 @@ NeighborhoodGraph::setupSeeds(NGT::SearchContainer &sc, ObjectDistances &seeds,
     ObjectDistance result;
     result.id = (*i).id;
     if (getObjectRepository().isEmpty(result.id)) {
-      cerr << "setupseeds:fatal error " << result.id << endl;
-      assert(0);
+      stringstream err;
+      err << "setupseeds:fatal error " << result.id;
+      NGTThrowException(err);
     }
     Distance d = objectSpace->getComparator()(sc.object, *getObjectRepository().get(result.id));
     result.distance = d;
